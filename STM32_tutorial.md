# 📚 Обучение программированию STM32 на C

Разбор проекта `main.c` для STM32F103C8T6 (Blue Pill) с подробными объяснениями.

---

## 📖 Оглавление

1. [Структура проекта](#1-структура-проекта)
2. [Заголовочные файлы](#2-заголовочные-файлы)
3. [Глобальные переменные](#3-глобальные-переменные)
4. [Функция задержки](#4-функция-задержки)
5. [Инициализация GPIO](#5-инициализация-gpio)
6. [Настройка прерываний EXTI](#6-настройка-прерываний-exti)
7. [Работа с кнопкой и светодиодом](#7-работа-с-кнопкой-и-светодиодом)
8. [Главный цикл](#8-главный-цикл)
9. [Обработчик прерывания](#9-обработчик-прерывания)
10. [Ключевые концепции](#10-ключевые-концепции)
11. [Практическое задание](#11-практическое-задание)

---

## 1. Структура проекта

```
bluepill_IRQ/
├── Core/
│   ├── Src/
│   │   ├── main.c              ← Главный файл программы
│   │   └── stm32f1xx_it.c      ← Обработчики прерываний
│   └── Inc/
│       └── stm32f1xx_it.h      ← Заголовок прерываний
├── Drivers/
│   └── CMSIS/
│       └── Device/ST/STM32F1xx/
│           └── Include/
│               └── stm32f103xb.h  ← Описания регистров STM32F103
└── Makefile                    ← Файл сборки
```

---

## 2. Заголовочные файлы

### Подключение описания регистров

```c
#include "../../../Drivers/CMSIS/Device/ST/STM32F1xx/Include/stm32f103xb.h"
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `#include` | Директива препроцессора для подключения заголовочного файла |
| `"..."` | Путь к файлу в кавычках — поиск начинается с текущей директории |
| `../../../` | Подняться на 3 уровня вверх из `Core/Src/` |
| `stm32f103xb.h` | Файл с описанием всех регистров МК STM32F103C8T6 |

**Что содержится в файле:**
- Адреса всех периферийных блоков (GPIO, RCC, EXTI, NVIC и т.д.)
- Структуры регистров
- Битовые маски для работы с отдельными битами
- Определения прерываний (IRQn)

**Почему именно этот файл:**
```
stm32f103xb.h
         ^
         └── "b" = 128 КБ Flash, "x" = 64 КБ SRAM
```

---

## 3. Глобальные переменные

```c
volatile uint8_t button_pressed = 0;
volatile uint8_t button_debounce = 0;
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `volatile` | Ключевое слово, запрещающее компилятору оптимизировать доступ к переменной |
| `uint8_t` | Тип данных: 8-битное беззнаковое целое (0...255) |
| `button_pressed` | Имя переменной: флаг нажатия кнопки |
| `= 0` | Начальная инициализация нулём |
| `;` | Конец оператора |

**Почему `volatile`?**

| Ситуация | Без volatile | С volatile |
|----------|--------------|------------|
| Переменная в прерывании | Компилятор может закэшировать значение в регистре | Компилятор читает из памяти каждый раз |
| Изменение в ISR | Основное приложение может не увидеть изменение | Изменение видно немедленно |

**Логика работы:**
```
button_pressed:
  0 — кнопка не нажата / не обработана
  1 — кнопка нажата, ожидает обработки

button_debounce:
  0 — антидребезг не активен
  1 — антидребезг активен (игнорируем новые прерывания)
```

---

## 4. Функция задержки

```c
void Delay(volatile uint32_t count) {
    while (count--) { __NOP(); }
}
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `void` | Функция не возвращает значение |
| `Delay` | Имя функции |
| `(volatile uint32_t count)` | Параметр: 32-битное число, количество итераций |
| `{` | Начало тела функции |
| `while (count--)` | Цикл: выполняется пока count ≠ 0, затем count уменьшается |
| `{ __NOP(); }` | Тело цикла: инструкция "No Operation" (пустая операция) |
| `}` | Конец тела функции |

**Как работает:**
```
__NOP() — инструкция процессора, которая ничего не делает
         но занимает 1 такт

При 72 МГц:
  count = 72 000 000 → задержка ~1 секунда
  count = 25 000     → задержка ~25 мс (антидребезг)
  count = 20 000     → задержка ~20 мс (антидребезг в Button_Read_Debounced)
```

---

## 5. Инициализация GPIO

### Функция `GPIO_Init()`

```c
void GPIO_Init(void) {

    RCC->APB2ENR |= RCC_APB2ENR_IOPAEN;
    RCC->APB2ENR |= RCC_APB2ENR_IOPBEN;

    /* PB2 - выход для светодиода */
    GPIOB->CRL &= ~GPIO_CRL_MODE2;
    GPIOB->CRL |= GPIO_CRL_MODE2;
    GPIOB->CRL &= ~GPIO_CRL_CNF2;
    GPIOB->BSRR = GPIO_BSRR_BR2;

    /* PA0 - вход с pull-up (кнопка на GND) */
    GPIOA->CRL &= ~GPIO_CRL_MODE0;
    GPIOA->CRL &= ~GPIO_CRL_CNF0;
    GPIOA->CRL |= GPIO_CRL_CNF0_1;
    GPIOA->BSRR = GPIO_BSRR_BS0;
}
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `void GPIO_Init(void)` | Объявление функции инициализации GPIO без параметров |
| `RCC->APB2ENR \|= RCC_APB2ENR_IOPAEN;` | Включение тактирования порта A (бит 2 регистра APB2ENR) |
| `RCC->APB2ENR \|= RCC_APB2ENR_IOPBEN;` | Включение тактирования порта B (бит 3 регистра APB2ENR) |
| `GPIOB->CRL &= ~GPIO_CRL_MODE2;` | Очистка битов MODE2 (биты 4-5 регистра CRL) |
| `GPIOB->CRL \|= GPIO_CRL_MODE2;` | Установка MODE2 = 11 (выход 50 МГц) |
| `GPIOB->CRL &= ~GPIO_CRL_CNF2;` | Установка CNF2 = 00 (Push-pull выход) |
| `GPIOB->BSRR = GPIO_BSRR_BR2;` | Сброс вывода PB2 (LED выключен, бит 18 = 1) |
| `GPIOA->CRL &= ~GPIO_CRL_MODE0;` | Очистка MODE0 (биты 0-1) — режим входа |
| `GPIOA->CRL &= ~GPIO_CRL_CNF0;` | Очистка CNF0 (биты 2-3) |
| `GPIOA->CRL \|= GPIO_CRL_CNF0_1;` | Установка CNF0 = 10 (вход с pull-up/pull-down) |
| `GPIOA->BSRR = GPIO_BSRR_BS0;` | Установка PA0 в 1 (включение pull-up, бит 0 = 1) |

### Тактирование RCC

**Аналогия:** Представьте, что периферия — это электроприборы в доме. Прежде чем использовать прибор, нужно включить автомат в щитке.

```c
RCC->APB2ENR |= RCC_APB2ENR_IOPAEN;
```

| Часть | Значение |
|-------|----------|
| `RCC` | Указатель на структуру регистров RCC (адрес 0x40021000) |
| `->` | Оператор доступа к полю структуры через указатель |
| `APB2ENR` | Регистр включения периферии на шине APB2 |
| `\|=` | Побитовое ИЛИ с присваиванием |
| `RCC_APB2ENR_IOPAEN` | Маска битa 2 (0x00000004) |

### Настройка режима вывода (PB2 — светодиод)

**Регистр CRL** (Configuration Register Low) настраивает пины 0-7.

**Битовая структура для каждого пина:**
```
Каждый пин: 4 бита
  Биты [1:0]   — MODE (режим и скорость)
  Биты [3:2]   — CNF (конфигурация)

Для PB2 (пин 2):
  Биты 5:4   — MODE2
  Биты 7:6   — CNF2
```

**Настройка MODE2:**
```c
GPIOB->CRL &= ~GPIO_CRL_MODE2;       /* Очистка MODE2[1:0] */
GPIOB->CRL |= GPIO_CRL_MODE2;        /* Установка MODE2 = 11 (50 МГц) */
```

| MODE[1:0] | Режим |
|-----------|-------|
| 00 | Вход (reset state) |
| 01 | Выход, 10 МГц |
| 10 | Выход, 50 МГц |
| 11 | Выход, 2 МГц |

> **Примечание:** `GPIO_CRL_MODE2` определён как `(GPIO_CRL_MODE2_0 | GPIO_CRL_MODE2_1)` — оба бита.

**Настройка CNF2:**
```c
GPIOB->CRL &= ~GPIO_CRL_CNF2;        /* CNF2 = 00 — выход push-pull */
```

| CNF[1:0] для выхода | Режим |
|---------------------|-------|
| 00 | Push-pull (двухтактный) |
| 01 | Open-drain (открытый сток) |
| 10 | Альтернативная функция Push-pull |
| 11 | Альтернативная функция Open-drain |

**Push-pull** — выход может выдавать и логический 0, и логическую 1.

### Настройка входа (PA0 — кнопка)

```c
GPIOA->CRL &= ~GPIO_CRL_MODE0;       /* MODE0 = 00 — вход */
GPIOA->CRL &= ~GPIO_CRL_CNF0;        /* Очистка CNF */
GPIOA->CRL |= GPIO_CRL_CNF0_1;       /* CNF0 = 10 — input pull-up/pull-down */
GPIOA->BSRR = GPIO_BSRR_BS0;         /* Включить pull-up */
```

| CNF[1:0] для входа | Режим |
|--------------------|-------|
| 00 | Analog mode |
| 01 | Floating input (плавающий вход) |
| 10 | Input with pull-up/pull-down |
| 11 | Reserved (зарезервировано) |

**Pull-up резистор** — внутренний резистор (~40 кОм), подтягивающий пин к +3.3V.

**Схема подключения кнопки:**
```
    +3.3V
      │
      ┌───┐
      │   │ Pull-up (внутренний)
      │   │ ~40 кОм
      └───┘
      │
PA0 ──┼───────┐
      │       │
      │      ┌┴┐
      │      │ │ Кнопка
      │      └┬┘
      │       │
GND ──────────┘

Когда кнопка разомкнута: PA0 = 1 (через pull-up)
Когда кнопка замкнута:   PA0 = 0 (соединено с GND)
```

**Почему BSRR, а не ODR?**
```c
GPIOA->BSRR = GPIO_BSRR_BS0;  /* Правильно — атомарная операция */
GPIOA->ODR |= GPIO_ODR_ODR0;  /* Тоже работает, но менее безопасно */
```

**BSRR** (Bit Set/Reset Register) — позволяет устанавливать и сбрасывать биты **атомарно** (за одну операцию, без гонки данных).

---

## 6. Настройка прерываний EXTI

### Функция `EXTI_Init()`

```c
void EXTI_Init(void) {

    RCC->APB2ENR |= RCC_APB2ENR_AFIOEN;

    AFIO->EXTICR[0] &= ~AFIO_EXTICR1_EXTI0;

    EXTI->FTSR |= EXTI_FTSR_FT0;
    EXTI->RTSR &= ~EXTI_RTSR_RT0;

    EXTI->IMR |= EXTI_IMR_MR0;

    NVIC_EnableIRQ(EXTI0_IRQn);
    NVIC_SetPriority(EXTI0_IRQn, 1);
}
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `void EXTI_Init(void)` | Объявление функции инициализации EXTI без параметров |
| `RCC->APB2ENR \|= RCC_APB2ENR_AFIOEN;` | Включение тактирования AFIO (бит 0 регистра APB2ENR) |
| `AFIO->EXTICR[0] &= ~AFIO_EXTICR1_EXTI0;` | Выбор PA0 как источника для EXTI0 (очистка битов = 00 = PA0) |
| `EXTI->FTSR \|= EXTI_FTSR_FT0;` | Разрешение прерывания по падающему фронту (бит 0) |
| `EXTI->RTSR &= ~EXTI_RTSR_RT0;` | Запрет прерывания по восходящему фронту (бит 0) |
| `EXTI->IMR \|= EXTI_IMR_MR0;` | Разрешение прерывания от линии 0 (бит 0) |
| `NVIC_EnableIRQ(EXTI0_IRQn);` | Разрешение прерывания EXTI0 в контроллере NVIC |
| `NVIC_SetPriority(EXTI0_IRQn, 1);` | Установка приоритета прерывания (1 из 15) |

### Шаг 1: Включение тактирования AFIO

```c
RCC->APB2ENR |= RCC_APB2ENR_AFIOEN;
```

**AFIO** (Alternate Function I/O) — блок альтернативных функций ввода-вывода.
Нужен для связи GPIO с EXTI.

### Шаг 2: Выбор источника для EXTI0

```c
AFIO->EXTICR[0] &= ~AFIO_EXTICR1_EXTI0;
```

**EXTICR** (External Interrupt Configuration Register) — выбирает, какой порт связать с линией EXTI.

| Значение | Порт |
|----------|------|
| 0000 | PA0 |
| 0001 | PB0 |
| 0010 | PC0 |
| 0011 | PD0 |

Мы оставляем 0 (PA0), поэтому только очищаем биты.

### Шаг 3: Настройка фронта прерывания

```c
EXTI->FTSR |= EXTI_FTSR_FT0;       /* Падающий фронт (1→0) */
EXTI->RTSR &= ~EXTI_RTSR_RT0;      /* Запрет восходящего */
```

**FTSR** (Falling Trigger Selection Register) — выбор падающего фронта.

**Падающий фронт** — переход сигнала с 1 на 0 (когда нажимаем кнопку, подключённую к GND).

**Диаграмма сигнала:**
```
Сигнал на PA0:

    1 ─────────────┐
                   │ нажатие (падающий фронт)
    0 ─────────────┘
                   ↑
              Прерывание!
```

### Шаг 4: Разрешение прерывания

```c
EXTI->IMR |= EXTI_IMR_MR0;
```

**IMR** (Interrupt Mask Register) — разрешает/запрещает прерывания от линий.

### Шаг 5: Настройка NVIC

```c
NVIC_EnableIRQ(EXTI0_IRQn);
NVIC_SetPriority(EXTI0_IRQn, 1);
```

**NVIC** (Nested Vectored Interrupt Controller) — контроллер прерываний Cortex-M3.

| Функция | Назначение |
|---------|------------|
| `NVIC_EnableIRQ()` | Разрешает конкретное прерывание |
| `NVIC_SetPriority()` | Устанавливает приоритет (0 = высший, 15 = низший) |

---

## 7. Работа с кнопкой и светодиодом

### Чтение кнопки

```c
uint8_t Button_Read(void) {

    return (GPIOA->IDR & GPIO_IDR_IDR0) ? 1 : 0;
}
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `uint8_t` | Тип возвращаемого значения: 8-битное беззнаковое целое |
| `Button_Read` | Имя функции |
| `(void)` | Функция не принимает параметров |
| `GPIOA->IDR` | Чтение регистра входных данных порта A |
| `& GPIO_IDR_IDR0` | Побитовое И с маской битa 0 (проверка только PA0) |
| `? 1 : 0` | Тернарный оператор: если не 0 → вернуть 1, иначе → 0 |
| `return` | Возврат значения из функции |

**IDR** (Input Data Register) — регистр, содержащий текущие уровни на входах.

**Как работает:**
```c
GPIOA->IDR          /* Читает все 16 пинов порта A (биты 0-15) */
GPIO_IDR_IDR0       /* Маска битa 0 (0x0001) */
&                   /* Побитовое И — проверяем только бит 0 */
```

**Результат:**
- Кнопка отпущена → PA0 = 1 (pull-up) → возвращается 1
- Кнопка нажата → PA0 = 0 (GND) → возвращается 0

### Чтение кнопки с антидребезгом

```c
uint8_t Button_Read_Debounced(void) {

    static uint8_t last_state = 0;
    uint8_t current = Button_Read();

    if (current != last_state) {
        Delay(20000);
        current = Button_Read();
        last_state = current;
    }
    return current;
}
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `static uint8_t last_state = 0;` | Статическая переменная, сохраняющая значение между вызовами |
| `uint8_t current = Button_Read();` | Чтение текущего состояния кнопки |
| `if (current != last_state)` | Проверка: изменилось ли состояние |
| `Delay(20000);` | Задержка ~20 мс для подавления дребезга |
| `current = Button_Read();` | Повторное чтение после задержки |
| `last_state = current;` | Сохранение текущего состояния |
| `return current;` | Возврат стабильного значения |

**Проблема дребезга:**
```
При нажатии кнопки механические контакты вибрируют

Решение: задержка 20-25 мс после первого изменения
```

### Управление светодиодом

```c
void LED_On(void)  { GPIOB->BSRR = GPIO_BSRR_BS2; }
void LED_Off(void) { GPIOB->BSRR = GPIO_BSRR_BR2; }
void LED_Toggle(void) { GPIOB->ODR ^= GPIO_ODR_ODR2; }
```

**Построчное описание:**

| Функция | Строка | Описание |
|---------|--------|----------|
| `LED_On` | `GPIOB->BSRR = GPIO_BSRR_BS2;` | Установка PB2 в 1 (бит 2 = 1) |
| `LED_Off` | `GPIOB->BSRR = GPIO_BSRR_BR2;` | Сброс PB2 в 0 (бит 18 = 1) |
| `LED_Toggle` | `GPIOB->ODR ^= GPIO_ODR_ODR2;` | Инверсия бита 2 регистра ODR |

| Регистр | Назначение |
|---------|------------|
| BSRR (Bit Set) | Установка бита в 1 (BS2, биты 0-15) |
| BSRR (Bit Reset) | Установка бита в 0 (BR2, биты 16-31) |
| ODR (Output Data) | Текущее состояние выходов |
| `^=` XOR | Инверсия бита |

**Почему BSRR использует разные биты для установки и сброса?**
```
BSRR (32 бита):
  Биты [15:0]   — установка (Set) битов ODR
  Биты [31:16]  — сброс (Reset) битов ODR

Пример для PB2:
  GPIO_BSRR_BS2  = (1 << 2)   = 0x00000004  → установить PB2
  GPIO_BSRR_BR2  = (1 << 18)  = 0x00040000  → сбросить PB2
```

---

## 8. Главный цикл

### Функция `main()`

```c
int main(void) {
    //volatile uint32_t delay = 300000;
    //uint8_t btn;

    GPIO_Init();
    EXTI_Init();

    __enable_irq();

    while (1) {

        /* ===== ТЕСТ 1: Мигание ===== */
        #if 0
        LED_On();
        Delay(delay);
        LED_Off();
        Delay(delay);
        #endif

        /* ===== ТЕСТ 2: LED горит при нажатой кнопке (опрос) ===== */
        #if 0
        btn = Button_Read();
        if (btn == 0) {
            LED_On();
        } else {
            LED_Off();
        }
        #endif

        /* ===== ТЕСТ 3: Переключение по кнопке (опрос с антидребезгом) ===== */
        #if 0
        if (Button_Read_Debounced() == 0) {
            LED_Toggle();
            Delay(200000);
        }
        #endif

        /* ===== ТЕСТ 4: Обработка через флаг из прерывания ===== */
        #if 1
        if (button_pressed) {
            button_pressed = 0;
            Delay(25000);
            if (Button_Read() == 0) {
                LED_Toggle();
            }
            button_debounce = 0;
        }
        #endif
    }
}
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `int main(void)` | Точка входа в программу |
| `GPIO_Init();` | Вызов функции инициализации GPIO |
| `EXTI_Init();` | Вызов функции инициализации EXTI |
| `__enable_irq();` | Встроенная функция: глобальное разрешение прерываний (CPSID instruction) |
| `while (1)` | Бесконечный главный цикл |
| `#if 0 ... #endif` | Условная компиляция: код исключается из сборки |
| `#if 1 ... #endif` | Условная компиляция: код включается в сборку |
| `if (button_pressed)` | Проверка флага нажатия кнопки |
| `button_pressed = 0;` | Сброс флага нажатия |
| `Delay(25000);` | Задержка ~25 мс для антидребезга |
| `if (Button_Read() == 0)` | Проверка: кнопка всё ещё нажата |
| `LED_Toggle();` | Переключение состояния светодиода |
| `button_debounce = 0;` | Сброс флага антидребезга |

### Последовательность инициализации

```
┌─────────────────────────────────────────┐
│  1. GPIO_Init()                         │
│     • Включить тактирование RCC         │
│     • Настроить PB2 как выход           │
│     • Настроить PA0 как вход с pull-up  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  2. EXTI_Init()                         │
│     • Включить AFIO                     │
│     • Связать PA0 с EXTI0               │
│     • Настроить падающий фронт          │
│     • Разрешить прерывание в EXTI       │
│     • Разрешить прерывание в NVIC       │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  3. __enable_irq()                      │
│     • Глобальное разрешение прерываний  │
│     • Специальная инструкция процессора │
└─────────────────────────────────────────┘
```

### Режимы тестирования

| ТЕСТ | Описание | Как включить |
|------|----------|--------------|
| 1 | Мигание светодиодом | `#if 1` в ТЕСТ 1, `#if 0` в ТЕСТ 4 |
| 2 | LED горит при нажатии | `#if 1` в ТЕСТ 2 |
| 3 | Переключение по кнопке (опрос) | `#if 1` в ТЕСТ 3 |
| 4 | Переключение по кнопке (прерывание) | `#if 1` в ТЕСТ 4 (по умолчанию) |

---

## 9. Обработчик прерывания

### Функция `EXTI0_IRQHandler()` (в stm32f1xx_it.c)

```c
void EXTI0_IRQHandler(void)
{
    if (EXTI->PR & EXTI_PR_PR0)
    {
        if (!button_debounce)
        {
            button_debounce = 1;
            button_pressed = 1;
        }

        EXTI->PR = EXTI_PR_PR0;
    }
}
```

**Построчное описание:**

| Строка | Описание |
|--------|----------|
| `void EXTI0_IRQHandler(void)` | Обработчик прерывания EXTI0 (имя должно совпадать с вектором) |
| `if (EXTI->PR & EXTI_PR_PR0)` | Проверка флага прерывания линии 0 |
| `if (!button_debounce)` | Проверка: антидребезг не активен |
| `button_debounce = 1;` | Установка флага антидребезга |
| `button_pressed = 1;` | Установка флага нажатия для обработки в main() |
| `EXTI->PR = EXTI_PR_PR0;` | Очистка флага прерывания (запись 1 в бит PR0) |

**Важно:** Флаг прерывания **обязательно** нужно очистить, иначе прерывание сработает снова немедленно!

**Сброс антидребезга** происходит в `main()`:
```c
if (button_pressed) {
    button_pressed = 0;
    Delay(25000);  // Антидребезг
    if (Button_Read() == 0) {
        LED_Toggle();
    }
    button_debounce = 0;  // ← Сброс после обработки
}
```

---

## 10. Ключевые концепции

### 1. Побитовые операции

| Операция | Синтаксис | Пример | Назначение |
|----------|-----------|--------|------------|
| И | `&` | `reg &= ~mask` | Очистить биты |
| ИЛИ | `\|` | `reg |= mask` | Установить биты |
| Исключающее ИЛИ | `^` | `reg ^= mask` | Инвертировать биты |
| НЕ | `~` | `~mask` | Инвертировать маску |

### 2. Работа с регистрами

```c
/* Установить биты (не трогая остальные) */
REG |= MASK;

/* Очистить биты (не трогая остальные) */
REG &= ~MASK;

/* Инвертировать биты */
REG ^= MASK;

/* Проверить бит */
if (REG & MASK) { ... }
```

### 3. volatile

```c
volatile uint8_t flag = 0;  /* Может измениться в прерывании */

/* Без volatile компилятор может сделать такую оптимизацию: */
if (flag) {         /* Прочитал один раз */
    if (flag) {     /* Использовал сохранённое значение! */
        // ...
    }
}

/* С volatile — читает каждый раз: */
if (flag) {         /* Прочитал из памяти */
    if (flag) {     /* Прочитал из памяти снова! */
        // ...
    }
}
```

### 4. Карта памяти STM32F103

```
Адресное пространство:

0x0000 0000 ──────────────────┐
                              │ Flash (программа)
0x0800 0000 ──────────────────┤
                              │
0x1FFF FFFF ──────────────────┤
                              │
0x2000 0000 ──────────────────┤ SRAM (переменные)
                              │
0x2000 4FFF ──────────────────┤
                              │
0x4000 0000 ──────────────────┤ Периферия
                              │
0x4001 0000 ──────────────────┤ GPIOA, GPIOB, ...
                              │
0x4002 1000 ──────────────────┤ RCC
                              │
0x4002 3000 ──────────────────┘
```

### 5. Тактирование периферии

Прежде чем использовать любой периферийный блок, нужно включить его тактирование:

```c
/* Шина APB2 (высокоскоростная периферия) */
RCC->APB2ENR |= RCC_APB2ENR_IOPAEN;   /* Порт A */
RCC->APB2ENR |= RCC_APB2ENR_IOPBEN;   /* Порт B */
RCC->APB2ENR |= RCC_APB2ENR_AFIOEN;   /* AFIO */

/* Шина APB1 (низкоскоростная периферия) */
RCC->APB1ENR |= RCC_APB1ENR_TIM2EN;   /* Таймер 2 */
RCC->APB1ENR |= RCC_APB1ENR_USART2EN; /* USART 2 */
```

### 6. Статические переменные

```c
static uint8_t last_state = 0;
```

| Характеристика | Описание |
|----------------|----------|
| Время жизни | Всё время работы программы |
| Область видимости | Только внутри функции |
| Инициализация | Только один раз при старте |
| Применение | Сохранение состояния между вызовами |

---

## 11. Практическое задание

Попробуйте самостоятельно:

### Задание 1: Изменить частоту мигания
```c
/* Найдите в main() и измените значение delay */
volatile uint32_t delay = 300000;  /* Попробуйте 100000 или 500000 */
```

### Задание 2: Добавить второй светодиод на PB3
```c
/* В GPIO_Init() добавьте настройку PB3 */
GPIOB->CRL &= ~GPIO_CRL_MODE3;
GPIOB->CRL |= GPIO_CRL_MODE3;      /* 50 МГц */
GPIOB->CRL &= ~GPIO_CRL_CNF3;      /* Push-pull */

/* Создайте функцию для второго светодиода */
void LED2_On(void)  { GPIOB->BSRR = GPIO_BSRR_BS3; }
void LED2_Off(void) { GPIOB->BSRR = GPIO_BSRR_BR3; }
void LED2_Toggle(void) { GPIOB->ODR ^= GPIO_ODR_ODR3; }
```

### Задание 3: Изменить приоритет прерывания
```c
/* В EXTI_Init() измените приоритет на максимальный */
NVIC_SetPriority(EXTI0_IRQn, 0);  /* 0 = высший приоритет */
```

### Задание 4: Добавить обработку отпускания кнопки
```c
/* В EXTI_Init() добавьте восходящий фронт */
EXTI->RTSR |= EXTI_RTSR_RT0;   /* Разрешить восходящий фронт */

/* В обработчике прерывания проверяйте какой фронт сработал */
if (EXTI->PR & EXTI_PR_PR0) {
    EXTI->PR = EXTI_PR_PR0;
    if (Button_Read() == 0) {
        /* Нажатие */
    } else {
        /* Отпускание */
    }
}
```

### Задание 5: Изменить антидребезг
```c
/* Измените задержку антидребезга в main() */
Delay(25000);   /* 25 мс */
Delay(50000);   /* 50 мс — для очень "грязных" кнопок */
```

---

## 📚 Полезные ресурсы

| Ресурс | Ссылка |
|--------|--------|
| Reference Manual (RM0008) | Полное описание STM32F103 |
| Datasheet | Электрические характеристики |
| ARM Cortex-M3 TRM | Документация на ядро |

### Поиск документации:
1. **Reference Manual** — введите в поиске "RM0008 reference manual"
2. **Datasheet** — введите "STM32F103C8 datasheet"
3. **CMSIS** — документация по стандарту ARM CMSIS

---

## 📋 Приложение: Быстрая справка

### Регистры GPIO

| Регистр | Назначение | Адрес (относительно GPIOB) |
|---------|------------|---------------------------|
| CRL | Конфигурация пинов 0-7 | 0x00 |
| CRH | Конфигурация пинов 8-15 | 0x04 |
| IDR | Входные данные | 0x08 |
| ODR | Выходные данные | 0x0C |
| BSRR | Установка/сброс битов | 0x10 |
| BRR | Сброс битов | 0x14 |
| LCKR | Блокировка конфигурации | 0x18 |

### Прерывания

| Регистр | Назначение | Адрес (относительно EXTI) |
|---------|------------|--------------------------|
| IMR | Маска прерываний | 0x00 |
| EMR | Маска событий | 0x04 |
| RTSR | Настройка восходящего фронта | 0x08 |
| FTSR | Настройка падающего фронта | 0x0C |
| SWIER | Программное прерывание | 0x10 |
| PR | Ожидающие прерывания | 0x14 |

### Полезные макросы

```c
/* Битовые операции */
#define SET_BIT(REG, BIT)     ((REG) |= (BIT))
#define CLEAR_BIT(REG, BIT)   ((REG) &= ~(BIT))
#define TOGGLE_BIT(REG, BIT)  ((REG) ^= (BIT))
#define READ_BIT(REG, BIT)    ((REG) & (BIT))

/* Проверка флага */
#define CHECK_FLAG(REG, FLAG) (((REG) & (FLAG)) != 0)
```

---

*Документ создан для изучения программирования STM32 на примере проекта bluepill_IRQ*
